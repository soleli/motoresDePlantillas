extends index.pug

block content
    div.container
        div.row.d-flex.justify-content-center
             div.col-lg-8
                 table(class="table")
                    thead
                        tr
                            th 
                            th Title
                            th Price
                            th Image
                    tbody
                        each product in productos
                            tr
                                th=product.id